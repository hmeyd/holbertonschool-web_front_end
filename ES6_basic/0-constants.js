function taskFirst() {
    var task = 'I prefer const when I can.';
    return task;
  }
  
function taskNext() {
    var combination = 'But sometimes let';
    combination += getLast();
  
    return combination;
  }